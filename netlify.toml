[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

[env]
  [env.production]
    # متغيرات الإنتاج تُضاف من Netlify Dashboard
    # لا تكتبها هنا في الملف!

[context.production]
  # لا توجد متغيرات هنا - استخدم Netlify Dashboard
